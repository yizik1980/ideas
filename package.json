{
  "name": "maze-monorepo",
  "private": true,
  "workspaces": [
    "maze-game",
    "libs/*"
  ],
  "scripts": {
    "start": "npm --workspace=maze-game run start",
    "build": "npm --workspace=maze-game run build"
  }
}
